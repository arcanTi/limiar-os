-r requirements.in
pytest
# Preferred transport for Starlette's TestClient, used throughout backend/tests.
httpx2>=2.9,<3
# Collect an honest backend baseline in CI before enforcing a minimum.
pytest-cov>=7,<8
# Usado por scripts/ruff-baseline.py, que roda no CI. A versao fica fixada
# porque uma release nova do ruff introduz regras e faria a baseline estourar
# por motivo alheio ao commit em revisao.
ruff==0.16.5
