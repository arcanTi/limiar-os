-r requirements.txt
pytest
# Usado por scripts/ruff-baseline.py, que roda no CI. A versao fica fixada
# porque uma release nova do ruff introduz regras e faria a baseline estourar
# por motivo alheio ao commit em revisao.
ruff==0.15.20
